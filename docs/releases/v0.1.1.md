# Zee-Cut v0.1.1 (Patch Release)

Release date: 2026-03-29

## Highlights

- Added safe target list management in GUI.
- Added lag presets for faster and safer bulk throttling workflows.
- Added diagnostics export to JSON from toolbar.
- Added structured scan logging and engine diagnostics snapshot.
- Added CI automation for lint and tests.
- Expanded unit tests for observability and scan summary behavior.

## Verification

- `python -m ruff check .`
- `python -m unittest discover -s tests -v`
- UI smoke run (safe list, preset apply, diagnostics export)
- Build artifact generated: `dist/Zee-Cut.exe`

